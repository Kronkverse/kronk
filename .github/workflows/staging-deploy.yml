name: Staging Deploy

on:
  workflow_dispatch:
    inputs:
      pr_number:
        description: 'PR number to deploy (admin use only)'
        required: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Deploy PR to staging
        env:
          DEPLOY_KEY: ${{ secrets.STAGING_DEPLOY_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          mkdir -p ~/.ssh
          echo "$DEPLOY_KEY" > ~/.ssh/key
          chmod 600 ~/.ssh/key
          ssh -o StrictHostKeyChecking=no -i ~/.ssh/key claude@170.64.182.29 "deploy-pr ${{ inputs.pr_number }}"
          ssh -o StrictHostKeyChecking=no -i ~/.ssh/key claude@170.64.182.29 "grep -q '^ANTHROPIC_API_KEY=' /home/mastodon/staging/.env.production || echo \"ANTHROPIC_API_KEY=$ANTHROPIC_API_KEY\" >> /home/mastodon/staging/.env.production; sudo systemctl restart mastodon-sidekiq mastodon-web"
          rm -f ~/.ssh/key
