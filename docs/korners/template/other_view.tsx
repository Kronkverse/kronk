// Second view — rendered at /hub/mykorner/other (the second entry in
// the manifest's `views:` list).

export const OtherView: React.FC = () => (
  <div className='mykorner__view'>
    <p>Other view — replace with the korner's actual content.</p>
  </div>
);
